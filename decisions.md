# Decisions

这里只记录会影响多个任务、模块或长期维护方式的决策。局部实现选择留在对应任务或源码中。

## 2026-08-06：精简工作台信息架构

**决定**：使用单一系统地图、扁平任务目录、按主题组织的知识文件和一份追加式决策日志；取消人类版与 AI 版知识的重复分层。

**原因**：原结构同时按受众、生命周期和内容类型分类，同一信息存在多个合法落点，容易产生重复、冲突和过期内容。

**影响**：

- AI 协作规则统一放在 Zephyr 根目录 `AGENTS.md`。
- `docs/` 与 `ai-context/` 中的系统知识合并为 `map.md` 和后续 `topics/`。
- 任务状态只使用 front matter，不再使用状态子目录。
- `progress/`、`data/`、独立模板和提示词卡片在没有真实消费者时不保留。

**复审条件**：真实专题数量、任务数量或自动化需求增长到扁平结构明显影响检索时，再按实际数据拆分，不提前预留层级。
